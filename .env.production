# ==================================
# 一剑轻安博客 - 生产环境配置
# ==================================
# 部署前请仔细阅读以下说明：
# 1. 将 your_server_ip 替换为你的腾讯云服务器公网 IP
# 2. 将 JWT_SECRET 替换为强随机字符串（使用 openssl rand -base64 32 生成）
# 3. 如果有域名，将 IP 替换为域名

# =================== 数据库配置 ===================
# SQLite 数据库文件路径（生产环境）
DATABASE_URL="file:./prod.db"

# =================== JWT 认证配置 ===================
# JWT 密钥（必须至少 32 个字符）
# ⚠️  重要：部署前必须修改为强随机字符串！
# 生成方法: openssl rand -base64 32
JWT_SECRET="CHANGE_THIS_TO_A_STRONG_RANDOM_KEY_USE_OPENSSL_RAND_BASE64_32"

# JWT 过期时间（默认 7 天）
JWT_EXPIRES_IN="7d"

# =================== CORS 配置 ===================
# 允许的跨域源（服务器 IP：114.132.201.84）
CORS_ORIGIN="http://114.132.201.84:3000"

# =================== 邮件配置（可选）===================
# SMTP 服务器配置（用于邮件订阅功能）
# 留空则禁用邮件功能
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="一剑轻安的博客 <noreply@yourdomain.com>"

# =================== 应用配置 ===================
# 应用 URL（用于生成 sitemap、RSS 等）
APP_URL="http://114.132.201.84:3000"

# 环境（development | production）
NODE_ENV="production"

# =================== 限流配置 ===================
# 限流配置（防止 API 滥用）
RATE_LIMIT_MAX="100"
RATE_LIMIT_WINDOW="60000"

# =================== 日志配置 ===================
# 日志级别（trace | debug | info | warn | error | fatal）
LOG_LEVEL="warn"
