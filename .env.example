# ==================================
# 一剑轻安博客 - 环境变量配置
# ==================================

# =================== 数据库配置 ===================
# SQLite 数据库文件路径
DATABASE_URL="file:./dev.db"

# =================== JWT 认证配置 ===================
# JWT 密钥（必须至少 32 个字符，生产环境请使用强随机字符串）
# 生成方法: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
JWT_SECRET="your-super-secret-key-change-this-at-least-32-characters-long"

# JWT 过期时间（默认 7 天）
JWT_EXPIRES_IN="7d"

# =================== CORS 配置 ===================
# 允许的跨域源（多个源用逗号分隔）
# 开发环境: http://localhost:3000
# 生产环境: https://yourdomain.com
CORS_ORIGIN="http://localhost:3000"

# =================== 邮件配置（可选）===================
# SMTP 服务器配置（用于邮件订阅功能）
# 留空则禁用邮件功能
SMTP_HOST=""
SMTP_PORT="587"
SMTP_USER=""
SMTP_PASS=""
SMTP_FROM="一剑轻安的博客 <noreply@blog.com>"

# =================== 应用配置 ===================
# 应用 URL（用于生成 sitemap、RSS 等）
APP_URL="http://localhost:3000"

# 环境（development | production）
NODE_ENV="development"

# =================== 限流配置 ===================
# 限流配置（防止 API 滥用）
RATE_LIMIT_MAX="100"
RATE_LIMIT_WINDOW="60000"

# =================== 日志配置 ===================
# 日志级别（trace | debug | info | warn | error | fatal）
LOG_LEVEL="info"
